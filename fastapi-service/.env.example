# FastAPI Service Environment Variables Template
# Copy this file to .env and update the values for your environment
# See config.py for validation and documentation

# Database Configuration (REQUIRED)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=123456
DB_NAME=4syz

# Database Configuration (Optional - has defaults)
DB_PORT=3306
DB_POOL_MAXSIZE=10
DB_POOL_MINSIZE=1

# CORS Configuration (Optional - has defaults)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Service Configuration (Optional - has defaults)
FASTAPI_DEBUG=False

# Security Configuration (Optional - has defaults)
RESTRICT_TO_LOCALHOST=True 