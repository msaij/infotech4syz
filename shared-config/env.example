# Unified Environment Configuration for 4syz Project
# Copy this file to .env in each service directory and update values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=123456
DB_NAME=4syz
DB_PORT=3306
DB_POOL_MAXSIZE=10
DB_POOL_MINSIZE=1

# =============================================================================
# SERVICE PORTS AND URLs
# =============================================================================
# Django REST API
DJANGO_PORT=8000
DJANGO_API_URL=http://localhost:8000
DJANGO_DEBUG=True

# FastAPI SSE Service
FASTAPI_PORT=8001
FASTAPI_URL=http://localhost:8001
FASTAPI_DEBUG=False

# Next.js Frontend
NEXTJS_PORT=3000
NEXTJS_URL=http://localhost:3000

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# Django Secret Key (generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
DJANGO_SECRET_KEY=django-insecure-x$157t-ol+jjd4aeh94pg*klk0%kk^h9+h6kjr516hk^#ofew*

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=5
JWT_REFRESH_TOKEN_LIFETIME=1
JWT_ALGORITHM=HS256

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:3000

# Security Settings
RESTRICT_TO_LOCALHOST=True
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=sample4syz@gmail.com
EMAIL_HOST_PASSWORD=sampleapppassword1234
DEFAULT_FROM_EMAIL=sample4syz@gmail.com

# =============================================================================
# SSE (Server-Sent Events) CONFIGURATION
# =============================================================================
SSE_UPDATE_INTERVAL=5
SSE_MAX_RETRIES=5
SSE_CONNECTION_TIMEOUT=30

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
LOG_LEVEL=INFO
LOG_FORMAT=json
ENABLE_REQUEST_LOGGING=True

# =============================================================================
# ENVIRONMENT
# =============================================================================
NODE_ENV=development
DJANGO_ENV=development
FASTAPI_ENV=development

# =============================================================================
# FEATURE FLAGS
# =============================================================================
ENABLE_SSE=True
ENABLE_REAL_TIME_UPDATES=True
ENABLE_AUDIT_LOGGING=True
ENABLE_PERFORMANCE_MONITORING=True 